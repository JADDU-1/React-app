import styled from '@emotion/styled';
import tw from 'tailwind.macro';


const TotalPage=styled.div`${tw`flex  justify-start `}`;
const SignOut=styled.button`${tw`m-2 h-10 p-1 w-4/12 border border-black rounded `}`;

export{TotalPage,SignOut}