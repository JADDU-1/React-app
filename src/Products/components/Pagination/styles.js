import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const ArrowButton = styled.button`
   ${tw`bg-black text-white p-2`}
`
const Text = styled.span`
   ${tw`p-1 `}
`

export { ArrowButton, Text }
