import styled from '@emotion/styled';
import tw from 'tailwind.macro';


const Total=styled.div`${tw`flex justify-between`}`;
const Title=styled.p`${tw` text-gray-600 `}`;
const Amount=styled.p`${tw` text-orange-500 `}`;

export{Total,Title,Amount}