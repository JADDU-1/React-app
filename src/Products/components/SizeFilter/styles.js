import styled from '@emotion/styled';
import tw from 'tailwind.macro';


const Sizes=styled.div`${tw`flex  flex-wrap justify-start `}`;
const SizeHeading=styled.p`${tw`font-bold`}`;
const SizeContainer=styled.div`${tw`flex flex-col w-full h-full`}`;

export{Sizes,SizeHeading,SizeContainer}